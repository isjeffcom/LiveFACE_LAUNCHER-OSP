<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical"
    android:fitsSystemWindows="true"
    >
    <ProgressBar 
	    android:id="@+id/pb_hide_shortcut"
	    android:layout_height="wrap_content"
	    android:layout_width="wrap_content"
	    android:visibility="gone"
	    android:layout_gravity="center_horizontal"
	    />
	<ListView 
		android:id="@android:id/list"
		android:layout_width="fill_parent"
    	android:layout_height="0dp"	 
    	android:layout_weight="1"
	    >
	</ListView>
	<LinearLayout 
		android:id="@+id/ll_hide_shortcut_bottom"
		android:layout_width="fill_parent"
		android:layout_height="wrap_content"
		android:layout_weight="0"	  
		android:orientation="horizontal"  
	    >

	    <TextView
	        android:id="@+id/tv_hide_shortcut_cancel"
	        android:layout_width="0dp"
	        android:layout_height="35dp"
	        android:layout_weight="1" 
	        android:gravity="center"
	        android:textSize="20sp"
	        android:background="@drawable/hide_shortcut_save_selector"
	        android:text="@string/hide_shortcut_cancel"/>

	    <TextView 
	        android:id="@+id/tv_hide_shortcut_save"
	        android:layout_width="0dp"
	        android:layout_height="35dp"
	        android:layout_weight="1"
	        android:textSize="20sp"
	        android:gravity="center"
	        android:background="@drawable/hide_shortcut_save_selector"
	        android:text="@string/hide_shortcut_save"
	        />
	</LinearLayout>		
</LinearLayout>